# 🚀 НАЧНИ ОТСЮДА — ввод в проект для нового ИИ

> Новый ИИ: прочитай этот файл целиком, затем последний снимок в `Сохранение/`,
> и продолжай по структуре. Не начинай заново, не переспрашивай уже решённое.
> Если доки расходятся с кодом — доверяй `git log` и реальному коду.

## 1. Что за проект
**TODO:** владелец ещё не описал проект. Спросить: что это, для кого, какой результат.
Пока известно только: это статический сайт (есть `index.html`).
**Папка проекта:** `/home/user/Labazan78` (репозиторий `labazan78`)
**Не путать с:** —

## 2. Что прочитать (по порядку)
1. Этот файл. 2. Последний снимок в `Сохранение/` (см. §8). 3. `ИСТОРИЯ-ВЕРСИЙ.md`. 4. `git log --oneline`.

## 3. Жёсткие правила
- Секреты — только в `.env` (он в `.gitignore`). Никогда в git/чат/скриншоты.
- Работаем маленькими шагами, после каждого — чекпоинт (см. §6).

## 4. Решения владельца (не передоговаривать)
- Облачная копия = **GitHub** (push в репозиторий). Google Диск не используется в облачном
  окружении (это вариант только для локального Mac).
- Система сохранения (скил + команда `/save`) ставится в `~/.claude/` — в облачном Claude Code
  она НЕ переживает пересоздание контейнера, переустанавливается из zip при необходимости.

## 5. Стек и запуск
- Стек: статический сайт (HTML/CSS/JS).
- **Запуск с нуля:** открыть `index.html` в браузере, либо локальный сервер:
  `python3 -m http.server 8000` → http://localhost:8000
- Где секреты: `.env` (в `.gitignore`). Сейчас секретов нет. Если появятся — на новой машине
  создать `.env` заново (в репозитории его нет).

## 6. Стиль и ритуал
- Владелец не программист — простой язык, проверять результат самому, маленькие шаги.
- **После каждого шага:** проверить → git-коммит → снимок `Сохранение/MX-*.md` →
  обновить §8 + `ИСТОРИЯ-ВЕРСИЙ.md` → `git push` (+ тег вехи `git push origin MX`).
  Одной командой: `/save MX "что сделал"`.
- `bash scripts/backup-to-drive.sh MX` — опционально, срабатывает только на Mac с Google Диском;
  в облаке корректно пропускается.
- Откат по имени тега (напр. «вернись к M1»).

## 7. ПОДВОДНЫЕ КАМНИ
- Облачный контейнер эфемерный: всё, что не закоммичено и не запушено, теряется. Коммить и пушь.
- `~/.claude/` (скил и `/save`) стирается между запусками — это нормально, переустановить из zip.

## 8. ГДЕ МЫ СЕЙЧАС
- **Готово:** установлена система сохранения (онбординг, снимки, git-теги, push в GitHub). Тег `M1`.
- **Последний снимок:** `Сохранение/M1-устанавливаю-save-system.md`
- **Следующий шаг:** владельцу — описать, что за проект `labazan78` (заполнить §1), затем начать работу.

---
Версия системы сохранения: см. `.save-system-version`.
